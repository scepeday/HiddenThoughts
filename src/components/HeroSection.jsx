export default function HeroSection() {
  return (
    <section className="hero">
      <div className="hero__copy">
        <h1>Objects for the private side of the room.</h1>
        <p>
          A stripped fashion and object storefront shaped by editorial layouts, hard contrast, and a controlled brand
          voice. The interface stays minimal so the catalog carries the weight.
        </p>
      </div>
    </section>
  );
}
